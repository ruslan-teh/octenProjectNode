const baseURL = 'https://jsonplaceholder.typicode.com'

export default baseURL

export const urls = {
    posts: '/posts',
    users: '/users',
    comments: '/comments',
    albums: '/albums',
    photos: '/photos'
}